# figma-cursors

Cursors for Figma

## Pre-requisites

- Node ([.nvmrc](./.nvmrc))
- Yarn

## Developing locally

1. Make sure you have the correct Node version
2. Install dependencies with `yarn`
3. Run `yarn dev`
